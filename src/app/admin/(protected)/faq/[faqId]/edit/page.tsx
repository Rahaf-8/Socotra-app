import Link from "next/link";import {notFound} from "next/navigation";import {FaqForm} from "@/components/admin/content/faq-form";import {requireAdmin} from "@/lib/auth/admin";import {answerBlocks,getAdminFaqCategories,getAdminFaqItemById} from "@/lib/content/faq-repository";
export default async function EditFaqPage({params,searchParams}:PageProps<"/admin/faq/[faqId]/edit">){await requireAdmin();const[{faqId},query]=await Promise.all([params,searchParams]);const[item,categories]=await Promise.all([getAdminFaqItemById(faqId),getAdminFaqCategories()]);if(!item)notFound();const value=(locale:"en"|"ar")=>{const t=item.translations.find(v=>v.locale===locale);if(!t)notFound();return{question:t.question,answer:answerBlocks(t.answer)}};return <section><nav className="text-sm"><Link href="/admin/faq" className="underline">FAQ</Link> / Edit</nav><h1 className="my-6 font-display text-5xl font-semibold">Edit FAQ item</h1>{"saved" in query?<p role="status" className="mb-6 rounded-xl border bg-white p-4 text-deep-ocean">FAQ saved.</p>:null}<FaqForm initialValue={{id:item.id,categoryId:item.categoryId,displayOrder:item.displayOrder,status:item.status,en:value("en"),ar:value("ar")}} categories={categories.map(c=>({id:c.id,label:c.translations.find(v=>v.locale==="en")?.label??c.key}))}/></section>}
