import type { TourItineraryDay } from "@/types/tour";
type Labels = { heading: string; day: string; overnight: string; location: string };
export function TourItineraryAccordion({ days, labels }: { days?: readonly TourItineraryDay[]; labels: Labels }) {
  if (!days?.length) return null;
  return <section aria-labelledby="itinerary-heading"><h2 id="itinerary-heading" className="font-display text-4xl font-semibold text-charcoal">{labels.heading}</h2><div className="mt-6 space-y-3">{days.map((day,index)=><details key={day.day} open={index===0} className="group rounded-2xl border border-warm-line bg-white p-5"><summary className="flex min-h-11 cursor-pointer list-none items-center justify-between gap-4 font-bold text-charcoal outline-none focus-visible:ring-2 focus-visible:ring-ocean"><span><span className="me-3 text-ocean">{labels.day} {day.day}</span>{day.title}</span><span aria-hidden="true" className="text-ocean group-open:rotate-45">+</span></summary><div className="pt-4 leading-7 text-charcoal/70"><p>{day.description}</p>{day.overnight?<p className="mt-3 text-sm"><strong className="text-charcoal">{labels.overnight}:</strong> {day.overnight}</p>:null}{day.location?<p className="mt-2 text-sm"><strong>{labels.location}:</strong> {day.location}</p>:null}</div></details>)}</div></section>;
}
